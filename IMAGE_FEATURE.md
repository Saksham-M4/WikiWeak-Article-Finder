# Wikipedia image feature

After a fact check, WikiFact Check requests the article's main Wikipedia page image using the Wikimedia API. If a page has no image, the fact check still works and simply omits the image.
